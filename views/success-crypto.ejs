<%- include("partials/header") %>

<main>
  <style>
    .success {
      padding: 60px 20px;
      background-color: #fff;
      text-align: center;
    }
    .success h1 {
      font-size: 2.5em;
      color: #f46d25;
      margin-bottom: 20px;
    }
    .success p {
      font-size: 1.2em;
      color: #444;
      margin-bottom: 15px;
      line-height: 1.5;
    }
    .success a {
      display: inline-block;
      margin-top: 20px;
      background-color: #f46d25;
      color: #fff;
      padding: 12px 30px;
      border-radius: 5px;
      text-decoration: none;
      transition: background 0.3s;
    }
    .success a:hover {
      background-color: #d75c1f;
    }
    @media (max-width: 768px) {
      .success h1 {
        font-size: 2em;
      }
      .success p {
        font-size: 1em;
      }
    }
  </style>

  <section class="success">
    <h1>Thank You for Your Donation!</h1>
    <% if (amount && org && crypto && txid) { %>
    <p>
      Your donation of $<%= amount %> USD to <%= org %> via <%=
      crypto.toUpperCase() %> has been submitted and is pending approval.
    </p>
    <p>Transaction ID: <%= txid %></p>
    <p>
      We’ll notify you at <strong><%= email %></strong> once your payment is
      confirmed.
    </p>
    <p>Thank you, <%= name %>, for supporting our cause!</p>
    <% } else { %>
    <p>Your donation has been submitted and is pending approval.</p>
    <p>
      We’ll notify you once your payment is confirmed. Thank you for supporting
      our cause!
    </p>
    <% } %>
    <a href="/">Return to Homepage</a>
  </section>
</main>

<%- include("partials/footer") %>
